/**
 * Version of the **public** JSON document shape produced by `modulens scan --format json`.
 * This is not the npm package version and not the internal `AnalysisSnapshot.snapshotVersion`.
 * Bump when adding/removing/renaming top-level sections or making other contract changes.
 */
export const PUBLIC_JSON_SCHEMA_VERSION = 2;
